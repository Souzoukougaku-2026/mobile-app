# 開発フィードバック & 順守事項 (AGENTS.md)

本プロジェクトにおける開発方針、および過去のフィードバックに基づく順守事項をここにまとめる。
開発者は本ファイルを常に確認し、修正のフィードバックループを回すこと。

## 1. アーキテクチャ原則 (MVVM + Repository Pattern)
- **UI (Compose)**: 描画に専念。ViewModel以外に依存しない。ビジネスロジック・API通信・DBアクセス禁止。
- **ViewModel**: UI状態 (State) 管理、イベント処理、Repository呼び出し。Contextへの依存禁止。
- **Repository**: データ取得の抽象化。API (Retrofit), DB (Room), DataStoreなどを吸収。
- **DataSource**: データの直接的な入出力 (Retrofit, Room等)。

## 2. 実装・コーディング規約
- **疎結合 & DI**: インスタンスの直接生成 (`new`) 禁止。`Dagger Hilt` を利用する。
- **SOLID原則**: 特に「単一責任」と「依存性逆転 (Interfaceへの依存)」を重視。
- **UI State**: 単一方向データフロー (UDF)。`StateFlow` を利用し、Immutableな `data class` で管理。
- **非同期処理**: `Coroutine` (`viewModelScope`, `suspend`, `Flow`) を利用。`GlobalScope` 禁止。
- **エラー処理**: UIに `try-catch` を書かない。Repositoryで `Result<T>` や `sealed class` に変換する。
- **クラス/関数サイズ**: 1クラス300行、1関数30〜50行を目安に分割。
- **ファイル構成**: 大規模なComposableは `components/` 配下に適切に分割して見通しを良くすること。
- **命名**: 意味のわかる名前を付ける (`tmp`, `data` 等の汎用名は禁止)。

## 3. UI・デザイン (Compose)
- **Stateless Composable**: 状態は極力ViewModelで保持し、Composableはステートレスにする。
- **Material 3**: テーマ (Color, Typography, Spacing) は `Theme` へ集約。
- **再利用性**: コンポーネントを適切に分割し、再利用可能な単位にする。

## 4. 過去の具体的フィードバックと修正
- **グラフ形式**: 棒グラフを採用。
    - **全体グラフ (Overall Graph)**: 全時間帯 (デフォルト48時間) を表示。12時間ごとのメモリを表示。20分ごとに1本の割合 (計144本) で描画。詳細グラフの表示範囲を半透明黄色でハイライト。
    - **詳細グラフ (Detailed Graph)**: 横スクロール可能 (表示幅デフォルト1時間)。15分ごとのメモリを表示。30秒ごとに1本の割合 (1時間あたり120本) で描画。シークバーは現在表示中の範囲に追従。
    - **可変性**: 時間軸の長さやバーの密度（本数）は定数や状態として可変に設計すること。
- **テーブル形式**: 縦並び形式 (項目名 | 値) を採用。リスト形式で区切り線を入れる。
- **レイアウト**: 
    - 上部: 16:9 プレースホルダ。
    - 下部: 左半分にズーム・パン対応の画像（独自座標系による初期表示指定可能）、右半分にテーブル。
- **画像操作**: ピンチズーム、ドラッグ移動をサポートし、画像が枠外に消えないよう制御すること。
- **データベース (Room)**:
    - 検索性能を最優先する。検索頻度の高い `realTime` などのカラムには適切なインデックスを貼ること。
    - 大量データを想定し、`LIMIT` によるページング（初期20件）を基本とする。
    - 正規化よりも検索速度を重視する場合、必要に応じて非正規化（冗長なデータ保持）を許容する。
- **パッケージ**: Feature First (`feature/name/presentation, domain, data`) を順守。

---
*本ファイルはフィードバックを受けるたびに更新し、開発プロセスに反映させる。*
